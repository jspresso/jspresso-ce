#############################################################
# Resource bundle property file for Portuguese translations #
#############################################################

# my.key=key translation
${parentArtifactId}.name=${applicationName}
${parentArtifactId}.description=${applicationDescription}
