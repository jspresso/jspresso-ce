class IoHelper
!!!410503.java!!!	IoHelper()

    // private constructor for helper class.
!!!410631.java!!!	copyStream(inout inputStream : InputStream, inout outputStream : OutputStream) : void

    byte[] b = new byte[1024];
    int n;
    while ((n = inputStream.read(b)) != -1) {
      outputStream.write(b, 0, n);
    }
