<project
  xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.jspresso</groupId>
    <artifactId>jspresso-root</artifactId>
    <version>3.0-SNAPSHOT</version>
  </parent>
  <groupId>org.jspresso</groupId>
  <artifactId>jspresso-flex</artifactId>
  <packaging>swc</packaging>
  <name>Flex</name>

  <dependencies>
    <dependency>
      <groupId>datetimelibrary</groupId>
      <artifactId>datetimelibrary</artifactId>
      <type>swc</type>
    </dependency>
    <dependency>
      <groupId>com.adobe.flex.framework</groupId>
      <artifactId>flex-framework</artifactId>
      <type>pom</type>
    </dependency>
  </dependencies>

  <build>
    <sourceDirectory>src/main/flex</sourceDirectory>
    <testSourceDirectory>src/test/flex</testSourceDirectory>
    <plugins>
      <plugin>
        <groupId>info.flex-mojos</groupId>
        <artifactId>flex-compiler-mojo</artifactId>
        <!-- don't know why but it doesn't take plugins management in m2eclipse -->
        <version>2.0.3</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>info.flex-mojos</groupId>
        <artifactId>asdoc-mojo</artifactId>
      </plugin>
      <plugin>
        <groupId>info.flex-mojos</groupId>
        <artifactId>flexbuilder-mojo</artifactId>
      </plugin>
    </plugins>
  </build>
</project>